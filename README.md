QRCgen is a simple app that use to generate QR code from text file.

-To create QR code from text file, the file must have string with line saperated. 

-For now it only generate QR code for simple strings. 
